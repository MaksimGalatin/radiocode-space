/**
 * КОММЕРЧЕСКИЕ САЙТЫ США — ЧИСЛА В ОДНОМ ЭКЗЕМПЛЯРЕ.
 *
 * ЧТО ЗДЕСЬ ЛЕЖИТ. Автоматическая проверка коммерческих сайтов США движком
 * axe-core: сколько формальных нарушений правил доступности находится на
 * странице, которую человек открывает в обычном браузере.
 *
 * 🔴 ЭТО ДРУГОЙ ПРИБОР, ЧЕМ НА СОСЕДНИХ СТРАНИЦАХ. И это главное, что нужно
 * сказать читателю до всякого числа.
 *
 *   /research/data    — клавиатурный обход ГОСУДАРСТВЕННЫХ сайтов: 95 524
 *                       записи, агент нажимает Tab как человек и смотрит,
 *                       дойдёт ли до цели. Поля `вердикт_человека`,
 *                       `шагов_до_цели`.
 *   эта страница      — axe-core по КОММЕРЧЕСКИМ сайтам: машина считает
 *                       нарушения правил в разметке и стилях.
 *
 * Складывать эти числа нельзя, и общего числа у них не бывает: они отвечают
 * на разные вопросы. Клавиатурный обход говорит «человек не дошёл»; axe
 * говорит «в коде нарушено правило». Одно не выводится из другого — ровно
 * поэтому расхождение между ними и стало главным результатом американского
 * исследования (53,8 %).
 *
 * ПОЧЕМУ СЮДА НЕ ПЕРЕНОСИТСЯ ИЗЪЯН ЕВРОПЕЙСКОГО НАБОРА. Из Европы отозваны
 * 71 065 записей: тот движок бил по клавиатуре синтетическим событием
 * `dispatchEvent`, которое не перемещает фокус. Здесь этого изъяна нет ПО
 * УСТРОЙСТВУ ПРИБОРА: axe-core разбирает дерево документа и вычисленные
 * стили, положение фокуса ему безразлично.
 *
 * ЗНАМЕНАТЕЛЬ НАЗВАН ВСЛУХ. Доли считаются от СТРОК ЖУРНАЛА. Одна
 * организация может дать несколько строк: у неё проверяется несколько
 * страниц.
 *
 * 🔴 ГДЕ ЛЕЖАТ ЖИВЫЕ ДАННЫЕ — ОПЛАЧЕНО ОШИБКОЙ 09.09.2026. Первый замер я
 * сделала по файлам `_ОБХОД/ОБХОД_США_ГЛАЗАМИ_W*` и получила 149 704 строки.
 * Число выглядело правдоподобно и было НЕВЕРНЫМ: те файлы стоят с 08:39, это
 * прошлый прогон. Текущий обход пишет на ДРУГОЙ диск и под другими именами —
 * `D:/_ОБХОД_ЖУРНАЛЫ/AXE_ЭТАП2_США_W1..W16.jsonl`. Нашлось это чтением
 * самого обходчика (`axe_stage2_master.py`), а не угадыванием по именам.
 * Правило на будущее: путь к данным берётся из кода, который их пишет.
 *
 * ЗАМЕР. Инструмент `_ОТЧЁТЫ/замер_этапа2.py`.
 */

/** Дата и время замера. Меняется вместе с числами, и только вместе с ними. */
export const СНИМОК = { год: 2026, месяц: 9, день: 9, часы: 23, минуты: 10 };

/**
 * 🟡 ОБХОД НА МОМЕНТ СНИМКА ЕЩЁ ИДЁТ: пройдено 125 831 из 133 165 (94,5 %).
 * Это сказано на странице прямо, а не умолчано: читатель должен знать, что
 * число вырастет. По завершении обхода правится ТОЛЬКО этот файл.
 */
export const ОБХОД_ИДЁТ = true;
export const ОЧЕРЕДЬ = { всего: 133165, пройдено: 125831, осталось: 7334, процент: 94.5 };

/** Главные числа страницы. */
export const Ч = {
  /** Строк в журнале обхода — знаменатель всех долей ниже. */
  строк: 125938,
  /** Битых строк. Ноль — и это проверяется каждым замером. */
  битых: 0,
  /** Уникальных организаций (по `osm_id`). */
  организаций: 125874,
  /** Записей, у которых сохранён снимок экрана как доказательство. */
  соСнимком: 115709,
  /** Их доля от строк журнала. */
  доляСоСнимком: 91.9,
  /** Сумма нарушений по всем записям. */
  нарушений: 499172,
  /** Из них по степени тяжести (шкала axe-core). */
  критических: 155416,
  серьёзных: 340315,
};

/**
 * Чем закончилась проверка. Доли — от строк журнала.
 *
 * «Измерено» — единственная группа, по которой вообще есть что сказать о
 * доступности. Остальные восемь исходов означают, что мерить было нечего, и
 * они НЕ считаются ни доступными, ни недоступными.
 */
export const ИСХОДЫ = [
  { ключ: 'measured', n: 86637, доля: 68.8 },
  { ключ: 'botwall', n: 20380, доля: 16.2 },
  { ключ: 'timeout', n: 7329, доля: 5.8 },
  { ключ: 'soft404', n: 3930, доля: 3.1 },
  { ключ: 'empty', n: 2944, доля: 2.3 },
  { ключ: 'silent', n: 2697, доля: 2.1 },
  { ключ: 'nodomain', n: 1799, доля: 1.4 },
  { ключ: 'brokentls', n: 218, доля: 0.2 },
];

/**
 * Соседние наборы — чтобы читатель не сложил несложимое. Числа здесь
 * ПОВТОРЯЮТ то, что лежит на своих страницах, и обновляются вместе с ними.
 */
export const СОСЕДНИЕ_НАБОРЫ = {
  /** Клавиатурный обход государственных сайтов США. */
  муниципалитетыЗаписей: 95524,
  муниципалитетыСайтов: 11659,
  /** Клавиатурный обход Германии и Испании. */
  европаЗаписей: 20833,
  европаДоменов: 9745,
  /** Отозванный европейский набор — называется прямо, а не умалчивается. */
  отозваноЗаписей: 71065,
  отозваноСтран: 10,
};
