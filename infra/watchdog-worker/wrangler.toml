name = "aifa-watchdog"
main = "worker.js"
compatibility_date = "2026-07-01"
workers_dev = true

# Проверка каждые пять минут. Это минимальный разумный шаг: чаще — лишний шум,
# реже — авария успеет продлиться заметно дольше, чем нужно.
[triggers]
crons = ["*/5 * * * *"]
